# Nagram Desktop 完整功能与配置设计

## 1. 范围与当前状态

本设计以 2026-09-13 本地源码中的增强功能、配置声明、设置页面、结构化配置和无开关操作为核对范围。Nagram iOS 与 Nagram Android 的普通 Telegram 功能不重复列为新增功能；README 中的历史功能说明只作为线索，配置和调用点优先。

当前 Qt 已完成品牌刷新、Nagram 设置页、P1-01～P1-08，以及本轮 P2-01～P2-11 的桌面实现。配置交换包含 115 个 D 偏好：96 个本机开关、19 个数值／文本／结构化对象；手机号遮罩、启动文件夹、过滤规则、本地别名及管理文件夹限制按账号保存。原生能力继续使用原入口，未实现的 P3 功能不展示空开关。各包交付范围、原生对应关系及验证限制见第 7 节。

[完整功能与配置目录](nagram-feature-catalog.md) 是本设计的组成部分：逐项列出基础配置的功能、原始类型/默认值、桌面去向；另列结构化模型、菜单动作、非配置功能与排除项。目录中的原始名称用于核对语义，**不是未来 Qt 存储键的承诺**，原始默认值也不是桌面默认值。

桌面范围约定：

- 排除双击消息动作及无编辑权限时的双击替代动作，不定制或覆盖 Qt 原生双击行为。
- 排除点击消息行代替长按、聊天左右滑、下拉归档、滑动进入画中画、手机返回手势、底栏布局、前后摄像头选择、振动/距离传感器、移动推送与厂商系统服务。
- 滚轮到达聊天边界后进入下一频道/话题的控制具有桌面意义，保留已接入的两项配置；不能和手机左右滑操作混为一项。
- 桌面已有的发送快捷键、撤销/重做、主题、自动下载、媒体解码、基础翻译、账号、代理等优先复用已有能力。原生入口不在 Nagram 页面重复添加。
- 适用性与实现成熟度分开：条件纳入项必须先验证 Qt/API/平台能力，不能因为有同名字段就显示为可用。

## 2. 信息架构与统一约定

保留当前六个顶层分组；随着消费者落地，在组内增加真实子页面。未实现功能只在设计中列出，不进入开关、快捷操作或搜索结果。

| 当前顶层分组 | 未来子页面及设计编号 |
| --- | --- |
| 外观 | F01 品牌与主题；F02 聊天列表与导航；F03 消息显示；F18 通知呈现 |
| 聊天 | F04 输入与文本；F05 消息菜单；F14 资料与管理 |
| 媒体 | F06 贴纸、媒体与音频；F13 消息截图 |
| 隐私 | F10 隐私与内容处理；F11 回执与在线状态；F12 本地消息历史 |
| 翻译 | F07 翻译与文本服务；F08 语音转写 |
| 高级 | F09 过滤规则；F15 网络与传输；F16 链接及外部集成；F17 配置管理与诊断 |

作用域缩写：`D` 本机安装数据目录；`A` 账号；`C` 对话；`T` 话题；`S` 当前操作/临时会话。默认外观为 D，网络身份、历史、规则及对话数据为 A/C/T。包含 Peer ID 的对象必须带账号，禁止跨账号串用。

所有新增行为默认关闭或继承 Qt 现状；显示型选项默认保持上游显示；比例默认 100%；排序默认上游；自定义集合默认不覆盖上游。下文范围值是桌面提案，不代表已通过性能验证。

配置页面显示当前值；共通的作用域、默认行为及生效方式集中说明。含义明确的开关只显示标题，不逐项重复描述；仅在存在重要范围、优先级、限制、账号差异或延迟生效时保留说明。数值项说明范围和继承语义。所有 Nagram 设置页面均不提供单项、分类或全局“恢复默认/重置”入口，后续新增设置也遵守此约定；用户直接修改原控件的值。即时刷新应通过现有响应式 UI 完成；影响解码器、字体或网络会话的变更须明确提示重新打开/重启。失败保留旧有效配置并显示原因，不静默降级或写回默认覆盖用户数据。

所有 Nagram 配置文案默认随版本内置英文、简体中文和繁体中文。新增标题、说明、分组和操作时，必须同步维护 `lang.strings` 中的英文，以及 `nagram_zh-hans.strings`、`nagram_zh-hant.strings`，确保键和占位符一致。简繁文本按当前界面语言及其基础语言选择；未覆盖的语言使用英文。内置文本作为缺失键的默认值，不写入云端语言缓存，也不覆盖语言包或自定义文件中显式提供的翻译。

当前 476 个 Nagram 文案均已覆盖三语，简繁资源的键和占位符与英文一致。语言代码先统一大小写和分隔符，再交给 Qt 识别脚本，兼容 `zh-hant`、`zh_TW`、`zh_Hant_HK` 等形式。既有本地化与菜单栏回归的 2060 项断言覆盖 11 种语言代码写法、逐键切换与缓存恢复、云端/自定义覆盖和删除回退、基础语言继承，以及菜单栏明暗外观下的 6 种未读计数布局；各批新增文案的验证记录见第 7 节。

## 3. 桌面功能设计

<a id="f01"></a>

### F01 品牌、主题与基础外观

D；复用 Qt 主题、缩放、主字体与图标选择。新增等宽字体、气泡／头像圆角、气泡尾部、引用颜色、主菜单标题／顺序／显隐／节日装饰、界面半角符号，以及忽略对话自定义主题和壁纸。圆角 0 表示继承，10–100 为主题半径百分比；字体为空时继承，非空必须是本机可用的固定宽度字体。字体和圆角在重启后应用，列表与消息仍使用同一套 Qt 样式。

主菜单头像沿用全局头像形状。开关样式保持 Qt 控件，不引入第二套控件库。核对调用点后更正两项适用性：Android `ProfileGalleryBlurView` 的大头像按钮模糊层在 Qt 中不存在，排除该选项；音乐封面自适应颜色依赖音乐封面获取模块，属于原有 P3-07 的封面后端范围。手机 Liquid Glass、软键盘、状态栏透明度、性能设备伪装不进入本组。

<a id="f02"></a>

### F02 聊天列表、文件夹与导航

D + A；会话相关选择按账号保存。

| 配置族 | 提议字段/类型与默认 | 行为及限制 |
| --- | --- | --- |
| 列表密度 | `compact=false`；预览行数 `inherit/1/2/3` | 统一行高与预览行数，不让两个开关互相静默改值 |
| 文件夹标签 | `display=inherit/text/icon/both`、`compact=false`、`hideUnreadCount=false` | 标签、未读计数、徽标分别控制 |
| 文件夹入口 | `hideAllChats=false`、`showArchiveInFolders=false` | 仅在存在可达替代文件夹时隐藏；可在 Nagram 原控件关闭该选项 |
| 初始位置 | `startup=default/last/specific`，默认 default；`folderId` 可空 | 账号隔离；文件夹失效时回到默认并提示，不删除选定 ID |
| 列表隐私 | 收藏/归档预览隐藏、隐藏通知数，默认关闭 | 仅影响本地显示，不改变归档或已读状态 |
| 社区分组 | `communityGrouping=inherit/off` | 仅在 Qt 有对应社区模型时提供 |
| 排序 | `sortOrder=[]`；候选未读、未静音、用户、联系人 | 有序规则去重；同分保持上游稳定顺序；不篡改服务端置顶顺序 |
| 类型过滤 | users/contacts/groups/channels/bots/admins/unmuted/unread/unmutedUnread 集合 | 优先对应上游文件夹规则；避免与消息正文过滤混用 |
| 最近会话 | `recentChats=false`、本机列表、清空动作 | 用侧栏/快捷菜单进入，排除长按标签触发；历史按账号隔离 |
| 主菜单 | 个人资料、机器人、新群、新频道、联系人、通话、收藏、主题、设置、代理、添加账号、最近转发 | 有序可见性集合，默认上游；设置入口始终可达 |
| 会话边界 | 禁止滚动进入下一频道/话题，默认 false | 鼠标滚轮/触控板适配，仍允许显式导航 |
| 聊天顶部工具栏 | `enabled=false`；动作集合 | 搜索、媒体、置顶、跳到开头、静音/取消静音、清缓存；复用 Qt 原生入口，清理仍需确认 |
| 阅读位置 | 保存滚动位置、返回历史、刷新保持最新边界 | 保存位置为账号+对话+话题状态；只有刷新前位于底部才保持底部 |
| 分享与加入 | 分享目标文件夹、最近转发、加入后选文件夹 | 桌面分享选择器可适配；系统 Share Extension 的手机专用处理排除 |
| 本地置顶 | 扩展本地置顶集合，默认空 | 不宣称提升服务端置顶上限；与服务端置顶明确区分 |

<a id="f03"></a>

### F03 消息显示与信息密度

D，必要时允许 A/C 覆盖。支持时间戳秒、转发原始时间、服务消息时间、消息 ID、精确数字、已编辑标记、发送者在线/最近在线提示、已读 tooltip、剧透直接显示、消息宽度、频道底部按钮、快速转发入口、简化引用、底部状态图标、反应可见性、动态与推荐入口。

- 时间戳秒与原始转发日期为 bool；时间格式跟随上游语言/时区。在线信息只显示 API 已提供的值，不推算被隐藏的精确在线状态。
- 消息宽度 `inherit` 或比例 50–400%，默认 inherit；`wideChannelPosts` 仅限频道，通用宽度倍率不能重复叠乘。
- 反应使用私聊/群组/频道三个可继承 bool；现有 `hideReactions` 为全局总开关，开启时隐藏全部，关闭时才读取分类覆盖。
- 频道底部按钮为 `inherit/hidden/mute/discuss`；无讨论群时保持可用的替代动作。输入框“发送身份”按钮归 F04；快速分享按钮与宽消息分别控制，不能复用一个含混键。
- 可见性集合覆盖相似频道折叠/隐藏、赞助消息/代理赞助入口、Premium 星标与头像动画、Stars 订阅提示、到期/升级/节日/恢复提示、生日、精选表情/贴纸/GIF、收藏表情标签/搜索标签、频道推荐、快捷标签、分享手机号提示、待办付费提示、空 Stars 按钮与礼物。默认沿用上游，仅控制本地呈现。

<a id="f04"></a>

### F04 输入、格式与文本处理

D + S。格式菜单集合包括粗体、斜体、等宽、删除线、下划线、引用、剧透、链接、提及、清除格式、撤销与重做。复用 Qt 文本实体和编辑栈；键盘快捷键与标准文字选择保持可用。

支持格式工具栏、Markdown 开关、Markdown 链接解析、默认代码语言、代码高亮入口、发送/编辑/阅读三个独立的中西文间距开关。阅读时只生成显示派生值，不修改消息原文；发送与编辑时保护代码、链接、提及及实体偏移，变换后须能撤销。

输入框按钮用可见性集合：附件、命令、表情、录音、自动删除、礼物、AI 编辑、机器人打开、发送身份；附件/表情悬浮弹窗独立 bool。隐藏按钮不禁用对应快捷键。输入占位文案可选择群名、当前发送身份，默认继承。点击 bot 命令“立即发送/填入草稿”默认继承，后者应允许用户审核。

`Enter/Ctrl+Enter` 发送方式直接使用 `Core::Settings::sendSubmitWay`，不新增 `nagram.sendWithReturnKey`。手机新 Markdown 解析器的版本开关不迁移，Qt 只保留一套解析实现。

<a id="f05"></a>

### F05 消息菜单、转发与批量操作

D 的有序动作目录 + S 的操作参数。动作的标准 ID、可用性和处理函数独立于可见性。可见性为 `inherit/hidden/visible/modifier`；modifier 在 macOS 用 Cmd/Option，在其他桌面平台用对应修饰键，并显示提示。隐藏不等于授权，操作仍检查会话权限、消息类型和当前状态。

完整动作目录见附录，包括回复、编辑、引用/无引用转发、复读/复制式复读、合并消息、反向回复、快捷评价文本、复制文本/图片/链接、翻译/朗读、收藏、媒体保存、媒体信息、下载清理、同作者搜索/选择、区间/全选、置顶/取消置顶、投票操作、计划消息、建议帖子、统计、举报、屏蔽、管理、消息详情、过滤、历史和截图。

- 复读确认、贴纸/GIF/语音/圆视频发送确认、通话确认均可独立设置，默认保持上游；不把菜单选中当作发送已成功。
- 转发策略：保留/隐藏来源、评论与转发先后顺序、快速选择目标、受保护消息复制式处理，须明确预览结果。
- 合并消息为 `off/text/forwardBundle` 等经 Qt 验证的枚举；不直接采用 Android 整数。批量收藏、取消置顶、同作者选择必须检查当前仍有效的消息集合，部分失效不能默默操作别的消息。
- 快捷评价支持两组自定义文本，默认空；填入草稿或明确发送动作，不自动回复。
- 危险动作（删除、举报、封禁）保留上游确认；偏好仅选择初始选项，不跳过确认。不迁移“跳过五秒撤销”的移动交互，Qt 的撤销机制保留。

<a id="f06"></a>

### F06 媒体、贴纸、音频与文件

D；路径与设备偏好本机保存。

| 配置族 | 类型与默认 | 桌面设计 |
| --- | --- | --- |
| 贴纸显示 | 消息尺寸 50–200%，面板缩放 100–400%，默认 100%；时间戳默认显示 | 统一 `stickerSize` 与 messageStickerScale；隐藏时间但保留可访问的消息详情 |
| 最近/收藏贴纸 | 最近数 `inherit` 或 1–200；扩展容量 false；收藏去重 inherit | “无限”映射到有明确上限的本地列表；不承诺突破服务端容量 |
| 贴纸组织 | 单贴纸收藏、置顶、只显示已添加、隐藏群贴纸、问候贴纸、精选入口 | 共用稳定文档/贴纸集 ID；备份/恢复/分享贴纸集目录为动作 |
| 表情与动画 | 系统/自定义表情资源、Premium 贴纸动画、远程表情互动、GIF 小图 | 本地资源需格式/版本验证；不引入 Android 字体或绘制器 |
| 发送媒体 | GIF 按视频、MP4 文件按视频、GIF 自动附带草稿说明、音视频确认 | 发送前可见预览，保留原始文件发送选择 |
| 播放 | 自动暂停、禁用自动播放、HLS 默认质量、解码器、预览与下载图标 | Qt 已有项复用；质量默认 auto，未知轨道不给不存在的选项 |
| 录制/通话 | 噪声抑制、语音增强、音频码率、视频码率、音频处理 | 使用现有音视频链路；默认 inherit，范围由实际编码器能力给出 |
| 元数据 | 分辨率、大小、类型、时长、帧率、码率、编码、文件名 | 未知字段显示未知，不能伪造；与消息详情共用入口 |
| 外部缓存 | 贴纸集导出目录、命名方式、自动刷新 | 条件纳入；桌面文件选择器、目录权限、失效提示；不迁移 Android URI 授权 |
| 音乐封面 | 可选封面服务地址 | 默认不请求；服务可用性单独验证，失败不影响音频播放 |
| 文件处理 | 保留原始文件名、清理单个下载、媒体保存/复制 | 复用上游下载和文件权限流程 |

<a id="f07"></a>

### F07 翻译、简繁转换与 LLM

D 的服务配置，A/C/T 的自动翻译覆盖，S 的发送前翻译目标。原有 Telegram 翻译与系统翻译优先复用；外部服务是显式选项，不能失败后自动切换服务发送内容。

服务候选完整覆盖：Telegram、系统原生、Google/Google CN/Google Cloud、Microsoft、Yandex、Transmart、DeepL/DeepL Free、可配置 DeepLX 和通用 LLM。候选不代表目前均可用；接入时分别验证请求格式、语言列表与错误处理。旧源代码中的模型名仅是当时默认值，不作为当前服务可用性推荐。

LLM 模型配置按 provider 实例保存：`id`、显示名、协议 `openai/anthropic/经验证的其他协议`、base URL、endpoint、model、凭据引用、系统提示词、翻译提示词、`useContext=false`、温度 `0…2`（默认 0.7，服务不支持时禁用）。模型候选兼容 OpenAI、Gemini、Groq、DeepSeek、xAI、Zhipu、Mistral、OpenRouter、Qwen、Moonshot、SiliconFlow、自定义；不为每家新建一套并行存储字段。支持模型列表读取、连接测试与错误信息，不能将测试直接发到聊天。

功能覆盖：消息/选中文本/输入框/Instant View 翻译、对话自动翻译、目标语言、原文与译文切换、发送前翻译回填草稿、简繁转换（阅读与输入分开）、正式程度、摘要入口。自动翻译为 `inherit/on/off`，覆盖顺序见第 5 节；不直接复刻 iOS “关闭即删除”的两态存储。

LLM 上下文默认关闭；开启时展示会带出的消息范围及截断规则，设置可取消的请求。翻译不得自动发送草稿；语言、实体、引用与代码格式需保留。密钥只进入系统凭据库，错误日志不含密钥或正文。

<a id="f08"></a>

### F08 语音转写

D/A；默认 Telegram，候选 OpenAI-compatible。字段：provider、baseURL、endpoint、model、language（空=自动）、prompt（空）、credentialRef。转写模型不得从翻译模型推断。支持连接/测试音频、模型选择、取消、失败重试、输入格式与大小校验；接口限制由 provider 能力返回，不硬编码手机录音路径。

语音与视频的音轨提取复用 Qt 媒体管线；外部上传前告知使用的服务。返回文本在转写区域显示，不代发消息；本地缓存按账号与媒体标识隔离，清理时移除转写缓存。

<a id="f09"></a>

### F09 过滤、屏蔽与规则

A/C/T；默认关闭，规则顺序为空。覆盖总开关、聊天内开关、发出消息的本地显示过滤、屏蔽用户消息、本地隐藏作者集合、Zalgo 组合字符过滤、规则导入/导出、全局规则的对话排除、临时显示被过滤内容。

提议 `Rule`：`id`、title、pattern、enabled、caseInsensitive、reversed、action（mask/replace/hide；旧 maskMessage 兼容为 hide）、replacement、authorPeerId 可空、accountId、peerId/threadId 可空、order。屏蔽和替换只改变显示；过滤发出消息不等于修改已发消息或阻止发送。

必须先编译并显示定位错误；提供样例输入和命中/替换预览。跨端 ICU/Foundation 正则不保证一致，Qt 采用实际引擎并报告不兼容项；防止灾难性回溯阻塞 UI。多规则次序、隐藏优先级、实体偏移与恢复原文都须有明确测试。规则导入保留原值和禁用状态，不自动启用。

<a id="f10"></a>

### F10 隐私、内容处理与本地锁

D/A。显示层：隐藏手机号、禁止联系人的默认手机号分享、演示隐私模式（隐藏身份/预览/通知）、本地账号隐藏、设置页锁、系统解锁。隐藏手机号只改变本机界面，不能替代服务端隐私设置。

内容层：敏感内容开关仅在 Telegram 返回可调整时显示；跳过已允许内容的重复确认不能绕过年龄/服务端校验。强制复制/本地内容保护处理为条件纳入，限定客户端已收到且能解码的数据，不承诺恢复未下载、过期或服务端拒绝的数据。

本地高级外观能力与订阅分开，不显示为已购买 Premium，不伪造服务端权益；可用能力逐项校验后呈现。账号/设置锁默认关闭；凭据不进入普通偏好或导出。应急锁定/清理为独立明确动作，先设计作用范围和恢复限制，不作为普通开关触发自动删除。

<a id="f11"></a>

### F11 已读回执、在线与活动状态

A，可选择共享策略；默认保持上游发送行为。字段：sendReadMessages=true、sendReadStories=true、sendOnline=true、sendActivity=true、offlineAfterOnline=false、markReadAfterAction=inherit、scheduledSend=false、silentSend=never、viewStoryPrompt=false、presetEnabled=false、lockedFields=空集合。

支持消息/动态回执、在线状态、上传/输入/选贴纸状态、补发离线包、操作后标已读、定时发送、静音发送 `never/preset/always`、查看动态前提示、策略预设及子项锁定。共享策略与独立账号策略互切必须预览受影响账号，关闭预设恢复之前配置，不覆盖锁定字段。

“仅本地全部已读”与“向服务端同步全部已读”为两个显式动作；本地缓存和网络回执队列分别管理。不能通过临时翻转全局 bool 执行网络操作，避免并发账号、异步任务或撤销导致回执意外发出。重启、断线重连、发送消息、手动标已读、观看动态都须覆盖；不承诺服务端绝对不可见。

F03 的 hidePrivateChatActivities 隐藏对方活动提示，和本组是否向对方发送活动状态完全独立。主菜单和托盘的策略入口仅在本组实现完成后加入。

<a id="f12"></a>

### F12 本地删除消息与编辑历史

A/C/T；默认不保存。独立设置保存删除消息、保存编辑历史、是否包含机器人、删除消息半透明样式、删除/编辑标记。支持查看版本、查看已删消息、单对话清理和全部本地清理，不能宣称恢复安装前或未接收的消息。

历史正文、实体、媒体引用不是普通偏好：进入单独版本化的账号数据存储，复用现有加密/会话生命周期设施。实现前确定保留期限、容量、媒体是否缓存、退出账号如何处理和清理行为；这些是启用条件。历史不得默认同步或随“配置导出”泄露。删除失败必须提示，不能把 UI 消失当成已清理。

<a id="f13"></a>

### F13 消息截图与分享图

D 的样式偏好 + S 的选区。支持截图入口、背景、日期、反应、头部装饰、彩色回复、剧透展开、内置/云主题、强调色。默认使用当前主题并保持剧透隐藏；分享前展示最终预览，用户可逐次覆盖。

云主题引用包含账号、themeId、documentId、title；accessHash 属于内部引用，不显示为普通设置、不跨账号复用、不导出为通用配置。支持复制图像/保存文件；隐藏头像/姓名等演示偏好应在预览中可见，截图尺寸和批量消息数需按实际内存预算验证。

<a id="f14"></a>

### F14 资料、群管理与可见数据

D + A/C。支持用户/会话 ID（hidden/Telegram API/Bot API）、消息 ID、DC、注册日期、完整简介、姓名顺序、频道身份标记及别名、彩色管理头衔、在线/最近在线提示、波斯日历及拉丁数字、通话前确认、匿名身份切换、群管理快捷入口、投票前票数（仅 API 已提供）。

注册日期来源若为估算或第三方，明确标注估算/来源与不可用状态，不当作 Telegram 权威字段。DC 信息只显示可获取值。频道别名按账号+peerId 存储，不修改远端名称。

群组资料页入口集合完整包括：群类型、邀请链接、关联频道、反应、外观、历史可见性、话题、位置、成员、权限、管理员、加入申请、已移除用户、最近操作、社区、删除群及其他已有管理项；按权限和 Qt 当前能力展示。

删除对话框的默认选择可以包含封禁、举报、删除该用户全部消息、共同群处理、删除用户反应，默认全不选，必须保留逐次确认。批量管理、无成员建群、升级群组、全部解除屏蔽等优先使用已有操作并明确目标集合。

<a id="f15"></a>

### F15 网络、代理与传输

D/A；默认上游网络策略。支持上传/下载性能档位、增强文件加载、IP 策略、系统 DNS/自定义 DoH、备用地址请求、代理自动切换、VPN 感知、代理订阅/自动更新、代理备注/测试/排序/失效节点处理、导入/导出/二维码，以及可执行文件/压缩包的自动下载例外。

性能档位提议 `default/balanced/fast`；实际分片大小、并发数与请求速率在 Qt 基准测试后确定，不照抄手机常量，不绕过服务端速率限制。自动下载例外合并到已有自动下载规则，不影响显式下载。目录选择复用上游下载/缓存配置。

SOCKS5/MTProxy 及现有代理能力优先复用。VMess/Shadowsocks/SSR/Trojan、订阅格式（SIP008、v2rayN、Clash 等）仅作为条件接入目标：先决定外部代理程序或可维护的后端，不因旧 README 声称支持就引入未维护核心。公共代理没有默认远程来源，不自动订阅。VPN 检测依平台能力，检测失败必须显示未知。

<a id="f16"></a>

### F16 链接、网页应用与外部集成

D/A。支持链接预览默认开关、规则修正 URL、自动 inline bot、本地/远程规则启停、外部浏览器打开规则、网页自动登录开关、链接确认、网页应用窗口尺寸、网页兼容身份、地图预览服务、话题标签搜索初始范围。

链接与 inline bot 使用不同规则集。预览替换只影响预览 URL，保留用户原始文本；bot 规则包含 username、patterns、enabled、来源/版本、顺序；本地启用集合与禁用的远程规则集合分别保存。远程更新展示来源和差异，不覆盖本地规则；默认不请求外部元数据。跳过媒体预览不能变成未经确认自动发送。

网页兼容身份为逐站点可选项，默认桌面身份；窗口宽高使用可调整比例并遵守屏幕工作区。敏感授权和 Telegram 登录确认不受“关闭普通链接提示”影响。Android 地图 SDK、OpenKeychain 应用绑定、手机厂商 Live Photo 写入不直接迁移；跨平台文件格式或 GPG 集成另行提出具体需求后设计。

<a id="f17"></a>

### F17 配置管理、同步、诊断与更新

D/A。提供搜索、修改项列表、配置导入导出、迁移报告、服务连接测试、版本及源码入口、诊断日志。云同步默认关闭；先做本地文件交换，再考虑受控云存储适配。iCloud 只可作为 macOS 可选后端，不能成为全平台基线。

导出使用版本化 allowlist，支持预览差异、未知项列表、类型/范围验证与整批原子提交。主题/规则/菜单模板与个人配置分开；密钥、账号会话、手机号、代理密码、锁凭据、消息历史、回执队列、最近会话、同步时间戳不进入默认导出。配置修改与导入只处理本设计管理的键，不清空登录和上游设置。

崩溃上传和自动更新当前在构建层关闭；启用前需独立服务、显式开关及数据范围说明。更新通道是发行配置；API ID/hash、测试 DC 是构建/开发配置，不作为普通用户运行时可调项。RPC 错误以可操作的错误码和说明呈现，不暴露凭据或正文。

<a id="f18"></a>

### F18 通知呈现

D + A；通知开关、内容预览、声音继续使用上游。新增可选项只覆盖延迟策略（inherit/immediate）、列表计数、应用角标、演示时隐藏内容及状态入口。通知延迟的关闭仅减少客户端可控制的等待，不能保证绕过操作系统调度。Android Bubbles、GCM/UnifiedPush、通知小图标资源不迁移。

## 4. 稳定键登记

当前本机交换集合包含 96 个布尔键和 19 个数值／文本／结构化对象，稳定键见 `Telegram/SourceFiles/nagram/nagram_settings.h`。账号级手机号遮罩复用上游能力，不属于本机交换集合。[本地配置文件规范](nagram-config-format.md) 定义这 115 项可交换偏好。

### 初始 27 键快照（历史记录）

以下为设置基础设施初次落地时的状态，不代表当前完成量。这些键当时已在 `Telegram/SourceFiles/nagram/nagram_settings.h` 声明，都是 D 作用域、`bool=false`。当时标为“已接入”的 20 项已生效，其余 7 项尚无消费者。当前状态以第 1 节和第 7 节为准。

| 稳定键 | 对应设计 |
| --- | --- |
| `nagram.chatListCompact` | F02 列表密度 |
| `nagram.hideStories` | F03 动态；已接入 |
| `nagram.hideReactions` | F03 全局反应隐藏；已接入 |
| `nagram.wideChannelPosts` | F03 频道消息宽度；已接入 |
| `nagram.secondsInMessages` | F03 时间戳；已接入 |
| `nagram.showForwardedMessageDate` | F03 转发时间；已接入 |
| `nagram.disableScrollToNextChannel` | F02 频道边界导航；已接入 |
| `nagram.disableScrollToNextTopic` | F02 话题边界导航；已接入 |
| `nagram.confirmCalls` | F14 私聊通话确认；已接入 |
| `nagram.showProfileId` | F14 ID 展示；已接入 |
| `nagram.showDC` | F14 头像 DC 展示；已接入 |
| `nagram.hideRecordingButton` | F04 输入按钮；当前代码分类 Media 保留；已接入 |
| `nagram.hideStickerTimestamp` | F06 贴纸时间；已接入 |
| `nagram.disableVideoAutoplay` | F06 自动播放；已接入 |
| `nagram.hidePhoneNumber` | F10 本地手机号隐藏；继续预留，当前剧透遮罩复用上游账号状态 |
| `nagram.hidePrivateChatActivities` | F03 对方活动提示；当前代码分类 Privacy 保留；已接入 |
| `nagram.translateBeforeSend` | F07 发送前翻译 |
| `nagram.autoTranslate` | F07 本机默认偏好 |
| `nagram.enablePanguOnSending` | F04 发送间距 |
| `nagram.enablePanguOnEditing` | F04 编辑间距 |
| `nagram.regexFiltersEnabled` | F09 总开关 |
| `nagram.showMessageId` | F03 时间提示中的消息 ID；已接入 |
| `nagram.hideQuickShare` | F03 快捷转发箭头；已接入 |
| `nagram.hideBotMenu` | F04 机器人菜单按钮；已接入 |
| `nagram.disableEmojiHover` | F04 表情面板悬停；已接入 |
| `nagram.disableAttachHover` | F04 附件菜单悬停；已接入 |
| `nagram.botCommandsToDraft` | F04 命令链接填入输入框；已接入 |

语义合并规则：iOS `stickerTimestamp=true` 与 Qt `hideStickerTimestamp=false` 方向相反；`hidePhoneInSettings/HidePhone` 映射到本地隐藏能力；各端自动翻译继承模型不同；同一个动作的菜单显示、动作参数与后台状态不能混成一个 bool。消息截图的 `showReactions` 与聊天反应不是同一作用域。所有转换必须按版本显式迁移，不按键名猜测。

## 5. 存储与接口设计

当前 `Nagram::Get/Value/Set(Core::Settings &, Option)` 复用 `readPref<bool>`、`writePref<bool>`、`clearPref` 与现有延迟保存。`Value` 在订阅时读取当前值，随后监听设置变更并去重，以同步已打开的页面；订阅不能超过 `Core::Settings` 生命周期。写默认值移除覆盖，无新增文件/缓存/二进制字段顺序；枚举顺序不作为持久化标识。所有调用在主线程和有效 `Core::Settings` 生命周期内执行。

结构化模块使用各自带版本的 JSON 对象，以 `QByteArray` 存入既有 KV；当前键、范围和迁移规则以[配置规范](nagram-config-format.md)为准。纯 bool 继续使用稳定键。密钥使用 macOS Keychain / Windows Credential Manager；其他平台当前明确报告凭据库不可用，不使用明文替代。账号启动文件夹、过滤、本地别名、管理文件夹 ID 依次追加在账号序列化尾部，每项独立检查尾部及旧值兼容。

可继承对象模型：`schemaVersion + accountId + peerId? + threadId? + overrides`。有效值解析为 T → C → A → D → 默认；仅适用于明确支持这些层级的选项。`null/缺失` 表示继承，false 表示显式关闭。清除某层覆盖不修改其他层。账号引用、菜单 action ID、规则 ID 用稳定标识，避免数组位置成为 ID。

全局反应隐藏、隐私预设等总开关是运行时策略，不销毁子项保存值。功能消费者是唯一执行行为的一层；设置页不触发网络回执、翻译、上传、清理或自动发送。新增热更新订阅只在实际消费者需要时提供。

结构化解析在边界检查类型、枚举、范围、URL、引用完整性；失败保留原始载荷并提供错误。升级迁移幂等，有备份/回退和未知字段保留策略；解析新版本失败不覆盖为默认。历史库、缓存、账号数据和配置存储分别管理生命周期。

<a id="priority"></a>

## 6. P1 / P2 / P3 实施优先级

本节替代原先按功能族编号排列的六批计划。优先级按**预期常用度、实现复杂度、依赖是否就绪和验证成本**共同判断；常用度是典型桌面聊天场景下的产品判断，没有使用遥测数据。复杂度只描述工程范围，不代表工期估算。

- **P1：先做。** 高频且局部，或可直接复用上游；优先形成可用的日常体验。
- **P2：随后做。** 常用但需要独立模块，或价值明确、使用面相对较窄；通常依赖 P1 的设置/动作基础。
- **P3：最后做。** 涉及协议状态、持久化历史、跨设备同步、外部服务生态或较强平台差异；先完成能力验证与专项设计。

复杂度：低＝局部 UI/已有 API 接入；中＝跨页面、文本实体、状态继承或结构化配置；高＝异步网络状态、敏感数据生命周期、复杂迁移、性能或跨平台后端。条件纳入项即使有优先级，也必须先满足原设计的能力门槛。排除项不分级、不进入队列；已完成的品牌和设置基础不重复安排。

### P1：日常可用与低成本增强

| 包 | 汇总功能 | 常用度 | 复杂度 | 对应设计与边界 |
| --- | --- | --- | --- | --- |
| P1-01 | 可用选项注册、搜索、生效/作用域说明 | 高频基础 | 低–中 | F17；复用已有设置基础，随首个消费者落地，不提前生成全部选项 |
| P1-02 | 时间戳秒、转发原始时间、消息/用户/会话 ID、DC、精确数字、隐藏本机手机号 | 高频 | 低 | F03/F10/F14；只显示已有数据，注册日期估算不在此包 |
| P1-03 | 隐藏动态、反应、赞助/推荐入口、会员装饰、频道底部按钮、快速分享与发送身份入口 | 高频 | 低–中 | F03/F04；单项可见性先做，细分覆盖和布局重排随后做 |
| P1-04 | 紧凑聊天列表、预览行数、文件夹标签文字/图标、隐藏计数/全部会话、归档入口 | 高频 | 中 | F02/F18；保留可达导航；不包含排序引擎、社区模型 |
| P1-05 | 输入按钮显隐、常用消息菜单项显隐、点击 bot 命令先填草稿 | 高频 | 低–中 | F04/F05/F16；复用现有动作，不含任意排序、modifier 可见性或新批量动作 |
| P1-06 | 通话、贴纸/GIF/语音发送确认，转发与评论顺序、问候贴纸控制 | 高频 | 低–中 | F05/F06/F14；圆形视频录制存在可用入口后才提供对应确认 |
| P1-07 | 贴纸尺寸/时间戳/最近数、禁用视频自动播放、复制图片、媒体信息、显式清理下载 | 高频 | 低–中 | F06；使用现有媒体管线；不扩展本地无限收藏或外部缓存 |
| P1-08 | 复用上游发送快捷键、格式/代码高亮、撤销重做、基础翻译、提醒、文件路径及已有管理入口 | 高频 | 低 | F04/F05/F07/F14/F15；只有入口或缺口适配，不重做已存在能力 |

建议包内顺序：P1-01 → P1-02/03/05 → P1-04/06/07；P1-08 随对应入口一起完成。一个包包含多个可独立交付的小项，不能为了包完整而提前打开未完成选项。

### P2：完整交互、内容处理与模块增强

| 包 | 汇总功能 | 常用度 | 复杂度 | 对应设计与依赖 |
| --- | --- | --- | --- | --- |
| P2-01 | 图标选择、字体、气泡/头像圆角、消息宽度、引用样式、主题与壁纸覆盖、装饰 | 中频 | 中 | F01/F03；复用主题系统，多缩放/深浅色验证 |
| P2-02 | 启动文件夹、最近会话、顶部工具栏、分享文件夹、排序/类型筛选、阅读位置、边界导航 | 高频 | 中 | F02；账号隔离和稳定排序；按 P1 导航能力扩展 |
| P2-03 | 菜单排序/修饰键显隐、格式工具栏、盘古间距、Markdown 偏好、输入提示和快捷回复 | 高频 | 中 | F04/F05；依赖动作目录与文本实体/撤销测试 |
| P2-04 | 复读、无引用转发、合并/反向回复、快捷评价、同作者/区间选择、批量收藏/取消置顶 | 中–高频 | 中 | F05；权限与消息失效检查、结果预览，不自动发送 |
| P2-05 | 多翻译服务、单次 LLM 翻译、模型/提示词配置、发送前翻译、原文切换、简繁转换 | 高频 | 中–高 | F07；先实现凭据存储、取消/失败处理；上下文及自动翻译覆盖属于 P3 |
| P2-06 | 基础自定义语音转写与系统原生翻译/AI 适配 | 中频 | 中–高 | F08/F07；在 P2-05 的服务配置基础上做，系统能力须单独探测 |
| P2-07 | 本地正则过滤、隐藏作者/已屏蔽用户、Zalgo、规则预览、单对话启停 | 中频 | 中–高 | F09；必须有正则性能、实体偏移与恢复原文测试；复杂继承/远程分发留 P3 |
| P2-08 | 消息截图、背景/日期/反应/剧透选项、复制与保存 | 中频 | 中 | F13；先用当前/内置主题和预览；云主题跨账号引用留 P3 |
| P2-09 | 演示隐私模式、通知计数/角标/延迟细分、可见在线提示、资料别名、群管理快捷项 | 中频 | 中 | F10/F18/F14；纯显示或已有操作；不改变回执/在线发送策略 |
| P2-10 | 贴纸组织/目录备份、系统/自定义表情、媒体发送策略、质量选项、链接预览开关和本地修正规则 | 中频 | 中 | F06/F16；按现有解码/发送能力提供选项，外部缓存和新后端留 P3 |
| P2-11 | 本地配置导入/导出、修改项列表、结构化配置迁移和诊断 | 中频基础 | 中 | F17；随第一个结构化模块完善 schema，不将迁移延后到模块发布之后；完整交换 UI 随后做 |

P2-03/04 依赖 P1-05；P2-05/06 共享服务配置及凭据基础，但保留不同模型和 API；P2-07/08/10 与 P2-11 共用版本化配置约定。共享依赖只做实际模块需要的部分。

### P3：复杂能力与平台扩展

| 包 | 汇总功能 | 常用度 | 复杂度 | 对应设计与进入条件 |
| --- | --- | --- | --- | --- |
| P3-01 | 已读/动态回执、在线/活动状态、补发离线、隐私预设与锁定、仅本地/服务端全部已读 | 特定用户高频 | 高 | F11；先设计异步回执队列、断线重连和多账号隔离，不能临时翻全局 bool |
| P3-02 | 删除消息保存、编辑历史、删除对话记录、媒体保留与清理 | 特定用户高频 | 高 | F12；须先明确加密存储、容量/期限、退出账号和删除保证 |
| P3-03 | 内容保护处理、本地高级外观能力、敏感内容特殊处理、账号/设置锁与应急操作 | 中频/特定场景 | 高 | F10；条件纳入，逐项验证权限与能力；不伪造服务端权益 |
| P3-04 | 自动翻译 A/C/T 继承、LLM 上下文/摘要、高级 provider 兼容及批量处理 | 中–高频 | 高 | F07/F08；基础 provider、凭据及配置迁移已稳定；控制正文范围、取消与缓存 |
| P3-05 | 全局/账号/对话/话题规则继承、远程规则、本地覆盖、inline bot 自动匹配与外部打开规则 | 中频 | 高 | F09/F16；先有本地规则与预览，禁止远程更新自动启用或自动发送 |
| P3-06 | 上传/下载调优、增强加载、DNS/IP/备用地址、VPN 感知、代理自动切换/订阅/新协议 | 特定用户高频 | 高 | F15；必须有 Qt 基准与维护后端，不照抄手机分片参数 |
| P3-07 | 外部贴纸缓存、封面 API、录制/通话处理、码率/解码器扩展、网页兼容身份、地图服务 | 低–中频 | 高 | F06/F16；外部程序、编码器和平台能力明确后再接入 |
| P3-08 | 跨设备云同步、云主题引用、独立更新/崩溃服务、发行通道 | 中频/维护场景 | 高 | F17/F13；本地 allowlist、迁移和凭据隔离已稳定，服务具备独立归属 |
| P3-09 | 社区/论坛特殊导航、本地扩展置顶/收藏、注册日期估算、跨群管理及其他低频高级项 | 低–中频 | 中–高 | F02/F06/F14；单独验证 Qt/API 支持及副作用，保留原设计的限制 |

高常用度不自动把复杂项提升到 P1：例如回执控制、历史保存和下载调优使用价值高，但错误会影响协议状态、数据或稳定性。可以先验证这些能力，但验证不等于实现或发布；不增加中间的 P0/P4 队列。

### 功能族覆盖与未点名细项的归属

下面定义各功能族的默认级别和明确例外，确保逐项目录中的细项均可归入 P1/P2/P3。按**具体功能**覆盖默认值，不按源端字段数量分配优先级；同一个功能族可跨级。

| 功能族 | 默认级别 | 例外/拆分依据 |
| --- | --- | --- |
| F01 品牌主题 | P2 | 已完成品牌不重复排期；无新增 P1；移动布局仍排除 |
| F02 列表导航 | P2 | 基础密度/标签/归档 P1-04；社区/论坛特殊导航和扩展置顶 P3-09 |
| F03 消息显示 | P1 | 自定义圆角/宽度/引用布局 P2-01；账号/对话显示覆盖归 P2-09，待继承模型稳定后接入 |
| F04 输入文本 | P2 | 按钮显隐/已有格式与快捷键复用 P1-05/08 |
| F05 菜单操作 | P2 | 既有动作显隐/发送确认/评论顺序 P1-05/06；依赖历史/回执等动作跟随对应 P3 模块 |
| F06 媒体贴纸 | P2 | 基础显示/确认/自动播放/复制/元数据 P1-06/07；外部缓存/音视频后端 P3-07，本地容量扩展 P3-09 |
| F07 翻译 | P2 | 上游已有翻译入口 P1-08；自动翻译继承/上下文/摘要和高级兼容 P3-04 |
| F08 转写 | P2 | 基础单服务转写 P2-06；高级批量/provider 兼容 P3-04 |
| F09 过滤 | P2 | 全层级覆盖和远程分发 P3-05 |
| F10 隐私内容 | P3 | 本机手机号隐藏 P1-02；演示隐私的纯显示部分 P2-09 |
| F11 回执在线 | P3 | 菜单/托盘入口跟随能力一起做，不单独提前发布 |
| F12 本地历史 | P3 | 历史样式与查看动作跟随存储消费者，不提前放空入口 |
| F13 截图 | P2 | 云主题跨账号引用 P3-08 |
| F14 资料管理 | P2 | 已有数据 ID/DC/精确数字、通话确认、已有管理入口复用 P1-02/06/08；注册估算/跨群操作 P3-09 |
| F15 网络传输 | P3 | 现有下载/缓存路径入口 P1-08；已有自动下载规则的文件类型例外 P2-10 |
| F16 链接集成 | P3 | 已有 bot 命令/按钮可见性 P1-05；普通链接确认/预览开关及本地 URL 修正规则 P2-10 |
| F17 配置管理 | P2 | 首批可用选项与搜索 P1-01；云同步/更新/崩溃服务 P3-08；内部和构建字段不变成开关 |
| F18 通知 | P2 | 已有通知开关复用、计数显隐 P1-04/08；演示模式/延迟细分 P2-09 |

## 7. 分级交付与验收

优先推进 P1；P2/P3 的前置验证可在对应模块接入前开展，但不能以“排进优先级”为由提前声明、启用或显示未实现功能。每批完成后更新本节的实际范围，不能将整个功能包提前标为完成。

### P2 完整桌面实现（2026-09-21）

11 个 P2 包的实现范围已收口。这里的完成数按设计包统计，不能用来源目录的 530 个声明作完成率分母。已有入口与新增能力分别记录如下；平台限制不伪装成可用开关。下文历史批次的数量仅代表当时状态。

| 包 | 新增实现 | 复用与边界 |
| --- | --- | --- |
| P2-01 | 等宽字体、气泡／头像圆角、消息宽度、尾部／引用／缩略图、忽略对话主题、界面半角符号、菜单标题与装饰 | 主字体、主题、壁纸、缩放、应用图标选择使用原入口；字体／圆角重启生效 |
| P2-02 | 每账号启动文件夹；未读／未静音／用户／联系人稳定排序；主菜单顺序和显隐；仅管理群／频道的本机文件夹限制 | 原生最近会话、类型过滤、分享文件夹、日期跳转、阅读位置和顶栏操作；服务端置顶顺序不变 |
| P2-03 | 14 类菜单排序、9 类 Option／Alt 显隐；发送／编辑／阅读间距；代码语言、两组快捷回复、输入提示、Markdown 偏好 | 原生格式菜单、键盘快捷键和撤销栈；保护文本实体及代码 |
| P2-04 | 批量合并／反序／去署名预览；文本／转发／收藏夹草稿；同作者已加载消息选择、快捷回复 | 原生区间选择与取消置顶；检查权限、草稿冲突与消息失效；不自动发送 |
| P2-05 | 多实例翻译、独立模型与提示词、模型查询／测试；原生消息翻译弹窗；草稿翻译预览／回填／撤销；阅读简繁转换和原文切换 | 凭据绑定实际服务接口；发送前通过原输入框菜单显式翻译草稿，最后由用户发送；自动翻译继承与上下文仍为 P3 |
| P2-06 | 自定义语音／圆视频转写、取消重试、原生内联展示、128 条会话缓存；系统翻译及系统 AI 能力适配 | macOS 系统 AI 使用本地模型且先预览，不能用时说明原因；本机不具备生成资格，未验证实际系统生成 |
| P2-07 | 账号正则过滤、原文／相册／隐藏链接／按钮内容匹配、作者／转发来源／已屏蔽机器人、Zalgo、对话排除、异步预览、规则模板交换和原文恢复 | 有匹配／时间／文本长度预算；分组媒体整体保护；不改写原消息，复杂继承和远程规则仍为 P3 |
| P2-08 | 当前／内置主题截图，背景／日期／头部／反应／剧透选项，复制和 PNG 保存 | 预览后消息、权限或过滤变化会使旧图片失效；完整相册和图像内存预算；原生文件面板未做自动点击 |
| P2-09 | 账号本地别名；演示模式请求系统排除窗口捕获并遮罩列表／通知／标题；独立应用角标；两类通知延迟；反应分类显隐 | 在线信息仅用 API 已提供的数据；群管理、统计方式和通知内容开关使用原入口；窗口捕获排除依赖平台与捕获工具，界面遮罩不覆盖打开的聊天／资料页；不支持的平台明确说明 |
| P2-10 | 贴纸目录备份／预览／已安装包排序；群贴纸区；GIF 播放控制；MP4 文件视频预览；链接预览偏好、URL 修正规则及确认 | 表情管理、媒体质量、自动下载和发送确认使用原能力；不自动安装贴纸，MP4 不转码；外部缓存和新后端仍为 P3 |
| P2-11 | 115 个 D 偏好的格式 2 快照、修改项、导入预览、冲突检查、原子应用、诊断；旧格式 1 迁移 | 结构化对象独立版本；未知键展示并跳过，未来版本拒绝；账号、消息和密钥不交换 |

原生能力核对（路径相对 `Telegram/SourceFiles/`）：

| 能力 | 现有实现 |
| --- | --- |
| 主字体／应用图标／表情包 | `settings/sections/settings_chat.cpp` 的 `ChooseFontBox`、`ManageSetsBox`；`settings/sections/settings_advanced.cpp` 的应用图标选择 |
| 最近会话 | `dialogs/dialogs_widget.cpp` 的 `RecentPeers`；`storage/storage_account.cpp` 按账号读写最近会话 |
| 分享目标文件夹／类型筛选 | `boxes/share_box.cpp` 文件夹条；`data/data_chat_filters.cpp` 的 contacts、noncontacts、groups、channels、bots、noMuted、noRead 标志 |
| 顶栏操作与阅读位置 | 原顶栏／消息菜单的搜索、媒体、静音、缓存；日期导航与 `History::scrollTopItem`、section memento 保持位置；加入后可从原会话菜单添加文件夹 |
| 输入格式／区间选择／批量取消置顶 | `chat_helpers/message_field.cpp`、原 `InputField` 格式与撤销；`history/view/history_view_context_menu.cpp` 与 `window/window_peer_menu.cpp` |
| 质量、自动下载、在线与管理 | 原媒体查看器／`SendFilesBox` 的质量选择，原自动下载编辑器；`dialogs/ui/dialogs_layout.cpp` 的在线指示及资料页／原群管理菜单 |

验证：完整离线场景在 125% 和 200% 缩放下各通过 620 项检查，均为 0 失败、78 张 PNG，合计 1240 项检查和 156 张 PNG。覆盖三语结构化配置预览的实际应用、服务模型选择、草稿防覆盖和撤销、分组媒体与过滤、截图变化后导出保护、主菜单布局、别名／演示模式、转写生命周期、MP4 原始字节保留及本地文件交易。重启检查验证等宽字体和圆角在账号测试清理前已读入。476 个 Nagram 文案的三语键、重复键和占位符检查通过。证据位于 `out/nagram-p2-complete125-evidence`、`out/nagram-p2-complete200-evidence`；临时测试场景已从产品源码移除，交付构建日志为 `out/nagram-p2-production-build.log`。

移除临时测试代码后的 Debug 构建通过；隔离启动日志确认 Qt 窗口及 QRhi 渲染表面初始化成功。链接器只签署了 Mach-O，最终本地应用包使用 `codesign --force --sign - --timestamp=none out/debug/Nagram.app` 补齐资源封装签名，`codesign --verify --deep --strict` 通过；这不是发行签名或公证。未来重新链接后如需检查完整应用包，应重新执行本地签名及验证。

离线测试使用独立目录、合成消息及 localhost 服务，未发送真实聊天消息、调用付费接口或替换现有账号目录。真实外部服务、系统 AI 生成、原生文件面板的完整交互及 Windows／其他桌面系统未作运行验收。已有 `libtlottie` 库仍报告高于链接目标的最低系统版本警告，本机运行通过，不能据此宣称旧版 macOS 已验收。

### 历史批次

### P2 本地配置管理（2026-09-21）

在现有 90 项设置基础上新增 4 个管理操作：已修改项列表、导出、导入预览、诊断报告。管理操作不计作新的偏好开关。完整文件覆盖 89 项本机偏好；账号级手机号遮罩仍由原生存储管理，不参与交换。

导入支持部分合并、差异预览、未知项提示、严格类型／范围校验和预览冲突检查。验证通过后一次提交所有变更，再统一刷新和延迟保存。导出检查原始存储并原子写入文件，错误不改原配置。报告只含版本、数量及无效键，不含账号、凭据、设置值或路径。新增 29 个三语键，总计 208 个。格式、边界及维护要求见[本地配置文件规范](nagram-config-format.md)。

P2-11 的本地文件交换和诊断流程已接入；结构化配置及其迁移仍跟随未来菜单／规则／服务模块交付，本包保持“部分完成”。P2-01、03、09、10 保持先前已交付细项。该批结束时按设计包计数：

| 级别 | 已完成 | 部分完成 | 未启动 | 总包数 |
| --- | --- | --- | --- | --- |
| P1 | 8 | 0 | 0 | 8 |
| P2 | 0 | 5 | 6 | 11 |
| P3 | 0 | 0 | 9 | 9 |

该批结束时有 20 包未全部完成，其中 5 包已有交付。这是功能包状态，不是工作量百分比；各包体量差异很大。

验收：Debug 构建通过；125% 与 200% 各 688 项断言通过，共 1376 项、26 张截图。覆盖 89 项序列化往返、逐项类型拒绝、范围边界、部分合并、原子刷新、未知项／凭据排除、无效存储保留与显式修正、文件大小和读写失败，以及三语搜索、修改列表、预览取消／应用、导出、诊断复制、过期预览拒绝。证据为 `out/nagram-config-evidence125/test_log.txt` 和 `out/nagram-config-evidence200/test_log.txt`。文件选择器返回值在临时测试覆盖中注入，实际文件读写、生产回调和 Qt 控件均执行；未自动化 macOS 原生文件选择面板本身。现有登录目录未参与测试。

本机 Xcode 27 的标准库与旧 range-v3 前置声明冲突，当前 Debug 构建缓存通过 `CMAKE_CXX_FLAGS` 与 `CMAKE_OBJCXX_FLAGS` 的 `-DMETA_NO_STD_FORWARD_DECLARATIONS` 使用该库已有的兼容开关；未修改第三方源码或全局工具链。重新配置既有构建目录仍需 `env -u QT`，受限环境下同时指定可写的 Clang／Swift module cache。


### P2 首批：设置精简、消息样式与输入偏好（2026-09-15）

移除 Nagram 页中的 5 个重复原生设置入口及其搜索条目，移除 35 条重复开关说明。统一说明本机作用域、默认关闭和即时生效；菜单重新打开后更新。有具体限制或不同生效时机的设置保留说明。后续同样不增加重复原生入口或恢复默认按钮。

新增 9 个开关和 1 个数值选项，累计 90 项可用设置；使用现有 KV 存储，默认关闭或继承。完整 P2 的 11 个功能包尚未全部完成：本批推进 P2-01、P2-03、P2-09、P2-10 的下列细项，其他项保持设计状态。

| 配置 | 稳定键 | 行为与覆盖 |
| --- | --- | --- |
| 文字消息宽度 | `nagram.messageWidth` | P2-01：0 继承；50–400 为主题最大气泡宽度的百分比，受窗口宽度限制。纯文字消息生效，显式倍率优先于加宽频道消息；媒体保持其专用尺寸。 |
| 隐藏气泡尾部 | `nagram.hideBubbleTail` | P2-01：去除收发气泡尾部，保留相连消息的圆角、选择和原文。 |
| 引用和回复使用主题配色 | `nagram.simpleQuotesAndReplies` | P2-01：引用式回复和普通回复使用主题颜色，去除发送者自定义配色和背景图案，保留引用文本、跳转及展开。普通正文引用块仍使用原主题布局。 |
| 隐藏回复缩略图 | `nagram.hideReplyThumbnails` | P2-01：移除回复条内媒体缩略图并回收其空间，不修改原始媒体。 |
| 隐藏私聊反应 | `nagram.hidePrivateReactions` | P2-09：隐藏私聊（含机器人和收藏夹）的反应徽标与悬停按钮，菜单操作保持原偏好。 |
| 隐藏群组反应 | `nagram.hideGroupReactions` | P2-09：普通群、超级群及其话题共用分类。 |
| 隐藏频道反应 | `nagram.hideChannelReactions` | P2-09：广播频道分类；三个分类开关关闭时继承全局，全局隐藏优先，不写回分类值或改变反应记录。 |
| 禁用自动 Markdown 格式 | `nagram.disableMarkdown` | P2-03：输入符号按原文发送，手动格式、粘贴实体和已有格式保留；既有字段允许的标签范围仍有效。 |
| 默认禁用链接预览 | `nagram.disableLinkPreview` | P2-10：输入时停止自动预览请求，发送草稿带无预览标志；手动选择的预览和显式移除的预览保持用户选择。 |
| 隐藏群组贴纸分区 | `nagram.hideGroupStickers` | P2-10：即时隐藏群贴纸及配置占位区，保留已安装贴纸、群贴纸设置和服务端数据。 |

文案内置简体、繁体、英文；当前三语共 179 个键。上述显示与输入偏好通过响应式通知刷新，不需要切换数据目录或重新登录。

本批验收：macOS arm64 Debug 构建通过；125% 和 200% 启动缩放各通过 465 项检查，均为 `SCENARIO_RESULT: PASS (failures: 0)`，证据位于 `out/nagram-p2-checked125/` 与 `out/nagram-p2-checked200/`。覆盖 90 个 bool 的默认值、序列化、实例隔离和去重通知；三语新增开关真实点击、90 个搜索项与重复入口移除；宽度输入的边界、保存、取消、持久化、窄窗口、频道优先级和媒体尺寸隔离；气泡尾部像素、回复配色与缩略图空间、分类反应、群贴纸即时显隐、Markdown 原文与手动格式保留，以及自动链接预览、手动预览、显式移除和在途请求切换。另检查浅色与隔离深色消息配色截图、三语 179 个键和占位符，以及菜单栏默认资源一致性。

验证使用隔离目录、虚拟账号和不可达代理；没有向真实聊天发送消息。临时场景已还原为空实现并重建交付 Debug 应用。现有 `out/debug/tdata` 和运行实例保留，重启现有应用即可加载本批功能。服务端消息送达、远程预览返回及 Windows/Linux 运行未作端到端验收。

### P1 完成清单（2026-09-15）

本次新增 30 个开关、2 个数值选项和 1 个文本选项，累计 80 项可用设置。这里按主设计的 8 个功能包验收，不用原始目录的 530 个声明作为完成率分母。源端同义选项合并，上游已有能力复用；P2/P3 和桌面排除项保持原分级。

| 功能包 | 实现状态 | 最终覆盖 |
| --- | --- | --- |
| P1-01 | 完成 | 75 个自定义开关、上游手机号遮罩和 4 个值选项；三语、搜索、当前值、本机作用域及必要的生效说明；原生设置使用原入口；无恢复默认按钮。 |
| P1-02 | 完成 | 秒、原始转发日期、消息与资料 ID、头像 DC、精确浏览/回复数、手机号遮罩；已有数据缺失时不推算。 |
| P1-03 | 完成 | 动态、反应、赞助消息、相似频道、会员标识/状态、商业推广和生日栏、代理赞助、贴纸/表情/GIF 推荐、收藏标签、资料礼物、手机号分享提示、待办入口及快捷 Stars；频道静音底栏、快速分享、发送身份按钮。 |
| P1-04 | 完成 | 紧凑布局、普通聊天 1～3 行预览、收藏/归档预览隐藏、文件夹未读计数、隐藏全部会话及自定义文件夹归档入口；文件夹文字/图标/组合和横竖布局使用上游页面。 |
| P1-05 | 完成 | 附件、表情、录制、命令、机器人菜单、自动删除、礼物、AI、发送身份按钮；置顶、举报、屏蔽、统计、复制链接、转发、翻译、选择、表情包来源等常用消息菜单项；命令链接先填草稿。 |
| P1-06 | 完成 | 私聊通话、贴纸、GIF、语音和可用圆视频预览确认；分享框及聊天转发草稿的转发/评论先后顺序；新私聊问候贴纸显隐。 |
| P1-07 | 完成 | 贴纸尺寸/时间戳/最近数、视频自动播放、已知媒体信息；复制图片、保存文件和确认后清理下载复用现有动作。 |
| P1-08 | 完成 | 上游聊天、文件夹、动画、通知、下载/路径页面沿用原入口与搜索；发送快捷键、格式/代码、撤销重做、翻译、提醒和权限相关管理动作复用原路径。 |

新增选项默认关闭；数值 0 表示继承，已编辑标记空文本表示继承。全部保存到现有偏好容器，不更换工作目录、不新增账号数据库、不清除原始数据。切换可见性不修改发送身份、草稿、消息实体、反应记录或服务端权限；转发顺序只作用于用户明确确认发送的当前操作。

| 本次实现 | 稳定键/行为边界 |
| --- | --- |
| 预览行数 | `nagram.chatPreviewLines`：0 或 1～3。普通无文件夹彩色标签的聊天使用对应行高；论坛、标签行和窄模式保持原布局，紧凑开关不改写预览设置。 |
| 最近贴纸数量 | `nagram.recentStickerLimit`：0 或 1～200，仅限制面板展示；不删除最近记录，也不扩展服务端收藏容量。 |
| 已编辑标记 | `nagram.editedMark`：单行 UTF-8，空值继承原翻译；隐藏已编辑标记优先。更新、重复计划和估算提示保持原义。 |
| 时间与剧透 | `showServiceTime` 为服务消息追加本地化时间，秒选项同样适用；`revealSpoilers` 覆盖文字及普通图片/视频，敏感、付费和限时媒体仍受原处理约束；`hideReadTime` 仅隐藏已读查询提示，不改变回执。 |
| 导航 | `hideAllChatsFolder` 只过滤横向/竖向显示列表，有可达文件夹才生效；排序写回时保留全部会话原位置，删除最后一个自定义文件夹后全部会话自动可见。`showArchiveInFolders` 在自定义文件夹加入归档入口，不改变文件夹匹配规则。 |
| 输入区 | `hideAttachButton`、`hideEmojiButton`、`hideSendAsButton` 和 `hideAiComposeButton` 更新可见性及可用宽度；编辑时的替换媒体入口、回复键盘和实际发送身份保持有效。 |
| 发送顺序 | `forwardBeforeComment` 默认沿用评论在前；开启后先排入转发，再排入评论。分享框各目标独立保持此顺序，普通草稿及骰子评论都遵守设置。 |
| 推荐与礼物 | 搜索广告在重新搜索时隐藏或恢复，正常结果保留。商业/生日栏、推荐贴纸/表情/GIF 即时刷新；会员、推荐频道、问候贴纸、资料礼物、快捷 Stars 等按各项说明重新打开页面。代理赞助在重新连接代理后生效。隐藏收藏标签保留已选过滤标签和现有标签的取消操作。 |
| 媒体信息 | `showMediaDetails` 在普通聊天及现代消息列表菜单中加入可复制详情，显示现有文件名、类型、分辨率、字节数和时长；未知字段不臆造，不探测编码器或发出额外媒体下载。 |

数值和文本载荷解析失败会记录具体偏好错误、使用标明的继承值并保留原载荷，不静默写回；用户可从原控件输入有效值修复。生效方式遵循页面共通说明，个别延迟生效的设置另行标注。

上游能力核对（相对 `Telegram/SourceFiles/`）：

| 能力 | 现有入口与消费者 |
| --- | --- |
| 文件夹标签与布局 | `settings/sections/settings_folders.cpp::BuildViewSection` 附近的 `folders/tab-view` 和 `ChatsFiltersTabsMode`，由 `ui/widgets/chat_filters_tabs_strip.cpp`、`window/window_filters_menu.cpp` 消费。 |
| 动态头像与动画 | `settings/settings_power_saving.cpp::PowerSavingBox`；原控制面板使用原 `PowerSaving` flags，Nagram 不重复添加导航入口。 |
| 输入快捷键与格式 | `settings/sections/settings_chat.cpp` 的 `sendSubmitWay`；`history/view/controls/history_view_compose_controls.cpp` 与上游输入框菜单、代码块编辑器、撤销栈。 |
| 基础翻译、提醒及管理 | 原消息菜单、发送按钮菜单、资料页；权限及服务可用性由上游决定，隐藏相关消息菜单入口不禁用其他入口。 |
| 图片复制与媒体保存 | `history/view/history_view_context_menu.cpp` 与 `history/history_inner_widget.cpp` 的 `lng_context_copy_image`、保存动作；仍遵守原内容及文件权限检查。 |
| 显式下载清理 | 下载列表菜单 `info/downloads/info_downloads_widget.cpp::fillTopBarMenu` 显示确认框后调用 `downloadManager().deleteAll()`；单个文件动作复用下载列表 Provider。设置开关不触发清理。 |
| 回复返回与阅读位置 | `history/history_widget.cpp` 的 `replyReturns`/`scrollTopItem` 和现代列表的 memento；复用当前对话/话题的既有返回栈和位置状态。 |
| 时间详细提示、状态图标 | 消息时间 tooltip、`history/view/history_view_bottom_info.cpp` 的时间和状态绘制；P1 不引入另一套消息页脚布局。 |

F03 逐项核对：I003/I011～I015/I069/I070、N008/N023/N082/N108/N109/N111～N113、A022/A023/A027/A028/A030～A032/A047/A056/A071/A101/A108/A120～A137、D036/D037/D040/D041/D059/D062/D095/D106/D107 均由上述实现或原生能力覆盖。D040 的相似频道折叠复用原卡片，D041 提供完整隐藏；N112 的菜单反应控制映射为显示/隐藏。D044/D051/D058/D060 的非原生消息布局归既定 P2-01；D054～D056 的分类反应覆盖归既定 P2-09；D100 的 mute/discuss 布局扩展归 P2-01，P1 只交付 hidden。N050 `DisableChatAction` 实际改变对外输入状态，原目录误记为 F03，现纠正至 F11/P3-01；它与 P1 的本地隐藏对方活动提示无关。

本次验收：macOS arm64 Debug 在 125% 和 200% 启动缩放下各通过 575 项检查，均为 `SCENARIO_RESULT: PASS (failures: 0)`。证据分别位于 `out/nagram-p1-evidence-verified125/` 和 `out/nagram-p1-evidence-verified200/`。覆盖全部 81 个 bool 的默认值、序列化与实例隔离、新增 30 个开关的三语真实点击、数值保存/取消/重开、UTF-8 已编辑标记、错误载荷保留、普通/紧凑预览绘制、读屏预览脱敏、隐藏全部会话与排序、服务消息时间、已知媒体信息、剧透偏好、本地转发/评论排队顺序、搜索广告显隐及正常结果保留、输入框空间回收与草稿保留。三语资源的 211 个键、占位符和无重复键检查通过，macOS 托盘源码及三份位图与官方 `272f6f5c2d29` 一致。

测试使用隔离的数据目录和虚拟账号；转发验证仅检查断网状态下的本地排队结果，没有向真实聊天发送消息。测试覆盖文件恢复为仓库原始空场景后重新构建交付 Debug 应用。现有 `out/debug/tdata`、原运行实例及 `/Applications/Nagram.app` 保持不变。服务端转发送达、多目标相册发送、真实通话/录制、远程广告素材和 Windows/Linux 运行未作端到端验收；上表“完成”指实现与所列本机验证，不宣称这些外部场景均已测试。

### 首批 P1：资料信息与通话确认

本批覆盖 P1-01 的首批选项注册、搜索和生效/作用域说明，P1-02 的资料 ID、头像 DC 和手机号遮罩，以及 P1-06 的私聊通话确认。其他细项继续按队列推进。

| 已接入项 | 行为与边界 |
| --- | --- |
| 显示资料 ID | 用户/机器人、普通群、超级群和频道资料显示可选择、可复制的原始 API ID，标签区分用户/群/频道；不显示 Qt 内部带类型位的 ID，也不转换成 Bot API 负数格式。话题资料显示所属群的 ID。开关即时刷新已打开资料页。 |
| 显示头像 DC | 仅读取当前头像 `StorageFileLocation` 中已知的 DC，换头像后刷新。缺少头像/位置时明确显示 Unknown；不推断账号注册机房或请求额外资料。 |
| 始终确认私聊通话 | 复用上游通话预览及取消流程，开启后聊天顶栏等原先可直接拨出的入口也先进入预览；音视频均适用。预览中的明确发起动作继续有效，不循环确认；群通话、来电、已在进行的通话保持原有流程。系统设备权限提示仍由上游处理。 |
| 手机号剧透遮罩 | 复用 `Main::SessionSettings::phoneNumberHidden`，设置与个人资料中的手机号可点击揭示、仍可复制。与上游手机号右键菜单双向同步，按本机账号保存；不承诺全应用脱敏，不改变服务端隐私。已有选择保留，默认 false。 |

前三项使用 D 作用域、默认 false；手机号遮罩为 A 作用域、默认 false。原先的恢复默认入口已按 2026-09-14 的页面约定移除。未实现功能没有开关或搜索条目。

验证记录：macOS arm64 Debug 完整构建通过；独立临时数据目录中应用启动、退出正常，43 项配置回归检查通过，覆盖序列化往返、独立设置实例/账号隔离、多订阅者同步、重复写入去重、单项重置、订阅生命周期及未接入键默认值。临时测试代码不进入提交。真实账号资料页的视觉/交互、搜索跳转、跨平台表现及通话端到端流程尚未验收；本批没有拨打真实电话。

### 第二批：聊天显示、导航与媒体偏好

新增 11 个可用开关，对应 P1-02/03/05/07 的细项，以及 F02 的边界导航和 F03 的频道文本宽度。加上首批后共 15 个可用开关；本批全部按本机 D 作用域保存、默认 false，提供搜索和即时生效；原先的恢复默认入口已移除。

| 已接入项 | 行为与边界 |
| --- | --- |
| 聊天列表隐藏动态 | 隐藏普通列表及归档列表顶部的动态条和展开提示；收起已展开区域并恢复正常滚动。资料页中的动态入口保留。 |
| 隐藏消息反应 | 隐藏消息反应徽标和悬停反应按钮，重新计算占用空间；保留消息数据和右键菜单的反应操作，恢复后重新显示。 |
| 加宽频道文本 | 频道中无媒体的文本消息使用聊天区域可用宽度；不改变私聊、群聊或媒体消息宽度，也不叠加通用宽度倍率。 |
| 时间戳秒 | 消息、编辑时间及原始转发时间显示秒；保留本地 12/24 小时格式，去除时间格式中的时区字段。 |
| 转发原始日期 | 使用已有 `originalDate` 作为转发消息的显示时间；时间戳 tooltip 保留转发时间。关闭后恢复上游收藏/转发时间规则，外部回复预览保持原样。 |
| 禁止滚动切换频道 | 关闭滚动到底后的下一频道切换，并取消已排队的滚动跳转；显式打开频道不受影响。关闭此选项时继承上游滚动偏好。 |
| 禁止滚动切换话题 | 独立控制下一论坛话题的滚动切换，取消展开提示和待跳转状态；显式打开话题不受影响。 |
| 隐藏录制按钮 | 普通聊天和话题等输入区域在空草稿时显示发送按钮；保留录音/圆视频快捷键及正在进行的录制、试听、发送和取消流程。 |
| 隐藏贴纸时间戳 | 隐藏贴纸的时间、发送状态区域和对应命中区域；其他消息类型保持原有显示。 |
| 禁用聊天视频自动播放 | 视频及圆视频不再自动播放，链接预览的自动启动入口也遵守设置；保留显式打开/播放，GIF 与贴纸继续使用原偏好。切换时重建消息视图以停止旧的自动播放。 |
| 隐藏私聊活动提示 | 隐藏私聊顶栏与会话列表中的对方输入、录制等提示；群聊提示保留，不修改自己的活动、在线状态或回执发送。 |

消息显示与视频偏好通过每个账号的 Data::Session 订阅刷新已存在视图。刷新前收集消息列表，避免视图重建时修改正在迭代的容器；每个账号独立管理订阅生命周期。显示偏好不改动消息原文、服务端反应或网络权限。

验证记录：macOS arm64 Debug 构建通过。独立测试目录中运行临时离线场景，1504 项断言通过，包含 711 套 Qt 语言/区域组合的默认时间格式与秒显示、配置序列化/重置/响应式去重、11 项搜索注册、频道宽度及私聊对照、转发日期切换、反应数据保留与恢复、贴纸信息区域和刷新通知、私聊/非私聊活动提示对照，以及视频自动初始化抑制与手动打开入口保留。测试场景使用临时本地数据和合成消息，不需要真实账号；测试代码已移除，交付 Debug 应用已重新构建。

验证边界：动态条与滚轮导航的真实交互、录制中切换、真实视频/圆视频解码播放、设置页多缩放和 Windows/Linux 表现仍需后续实测。本批不宣称这些端到端场景已验收。仍预留的 7 个稳定键为紧凑列表、手机号备用键、发送前翻译、自动翻译、发送/编辑中西文间距和过滤总开关。

### 第三批：消息信息与输入偏好

新增 6 个可用开关，归入 P1-02/03/05。均按本机 D 作用域保存，默认 false，支持搜索和即时切换。本批移除所有既有恢复默认按钮及对应文案，不再增加此类入口；默认值仍在说明中体现，直接关闭开关即可恢复上游行为。

| 已接入项 | 行为与边界 |
| --- | --- |
| 时间提示中的消息 ID | 消息时间戳 tooltip 显示当前消息的服务端 ID；不显示待发送、本地或计划消息的内部 ID，不推算转发来源 ID。 |
| 隐藏快捷转发 | 隐藏消息旁的转发箭头及命中区域；消息菜单转发、评论及跳转到原消息仍可使用。切换刷新已打开消息视图。 |
| 隐藏机器人菜单按钮 | 隐藏机器人私聊输入框旁的菜单或小程序按钮，回收布局空间；不改变命令数据、手动输入 / 的补全和机器人回复键盘。 |
| 禁止表情悬停弹出 | 鼠标进入表情按钮时不再自动弹出表情、贴纸和 GIF 面板，点击入口保留；已经打开的面板仍可正常操作。 |
| 禁止附件悬停弹出 | 取消附件按钮的悬停菜单触发，切换时更新当前聊天的菜单；保留原有点击、拖放与快捷键。与表情悬停独立；上游实验性的统一点击模式继续优先。 |
| 命令链接先填输入框 | 普通聊天、话题及计划消息页面中的命令链接先插入输入框光标处，保留未选中的草稿和文本实体，支持撤销；复用上游群内机器人用户名解析。输入区域不可编辑时不发送命令。回复键盘和显式发送操作保持上游流程。 |

验证记录：macOS arm64 Debug 构建通过。独立临时数据目录中的 177 项检查通过，覆盖新配置默认值、序列化往返、独立设置实例、去重通知和开关互不影响；三语搜索注册及全部 21 个开关开启后的页面构建，均只有 21 个开关和源码链接，没有重置按钮；消息 ID 及本地 ID 排除、快捷转发区域与转发权限、机器人菜单即时显隐、Unicode 草稿与实体偏移、单步撤销、不可编辑输入和表情悬停/显式打开。71 条内置文案的三语键和占位符一致。临时测试代码已移除。

验证边界：附件菜单在真实已安装机器人环境中的悬停/点击、各类消息命令链接的完整导航与发送流程、多缩放截图及 Windows/Linux 运行尚未验收；本批未发送真实消息，也未打开机器人小程序。

### 第四批：显示细节、输入入口与消息菜单

新增 20 个可用开关，覆盖 P1-02/03/04/05/07 的局部显示与交互项，累计 41 个。全部使用 D 作用域、默认 false，提供英文、简体中文、繁体中文标题、说明及搜索条目。显示和输入区偏好即时刷新；菜单偏好在重新打开菜单时生效。沿用既有存储与响应式通知，不更换数据目录，不添加恢复默认入口。

| 已接入项 | 稳定键 | 行为与边界 |
| --- | --- | --- |
| 隐藏已编辑标记 | `nagram.hideEditedBadge` | 移除消息时间旁的普通已编辑标记，保留时间提示中的编辑日期；更新、估算及重复计划发送提示仍按原规则显示。 |
| 精确消息计数 | `nagram.exactMessageCounters` | 消息底部浏览量和回复数使用完整本地化数字；不补取服务器没有提供的计数，待发送消息仍不显示浏览量。 |
| 隐藏消息浏览量 | `nagram.hideMessageViews` | 同时移除浏览图标、数字及其布局空间，回复计数保留；不改动服务端计数或统计权限。 |
| 隐藏频道消息签名 | `nagram.hideChannelSignature` | 隐藏时间旁的作者签名，时间提示仍可查看完整签名；保留原始消息数据。 |
| 隐藏文件夹未读计数 | `nagram.hideFolderUnreadCounters` | 同时覆盖横向标签和垂直侧栏，移除相应辅助功能计数；隐藏期间继续接收未读更新，关闭后显示最新计数，不改变会话未读状态或通知。 |
| 隐藏输入区礼物按钮 | `nagram.hideGiftButton` | 即时销毁或恢复输入框旁的礼物按钮并更新布局；恢复时重新检查原有生日及发送资格，其他礼物入口保留。 |
| 隐藏命令快捷按钮 | `nagram.hideBotCommandButton` | 隐藏输入区斜杠快捷按钮并回收宽度；手动输入命令和回复键盘保留，与机器人菜单开关独立。 |
| 隐藏自动删除快捷按钮 | `nagram.hideAutoDeleteButton` | 隐藏输入区计时器并回收空间，不改变对话自动删除时间；资料页设置保留。 |
| 关闭 Premium 贴纸特效 | `nagram.disablePremiumStickerEffects` | 阻止贴纸外围特效并停止正在显示的对应特效；贴纸本体按原播放偏好工作。 |
| 关闭表情互动特效 | `nagram.disableEmojiInteractions` | 阻止收到和点击触发的本地表情互动，停止对应播放并清除待播互动；保留表情消息。 |
| 关闭消息特效 | `nagram.disableMessageEffects` | 阻止自动及手动触发的消息附带特效，清理对应播放、待播项及下载完成订阅；保留消息内容、特效元数据和已读生命周期。 |
| 菜单隐藏置顶/取消置顶 | `nagram.hideMenuPin` | 覆盖单条及适用的多选取消置顶入口；原有置顶权限和其他页面操作不变。 |
| 菜单隐藏举报 | `nagram.hideMenuReport` | 覆盖普通及限时消息举报；赞助消息和资料页的举报入口保留。 |
| 菜单隐藏屏蔽发送者 | `nagram.hideMenuBlock` | 控制回复通知中的屏蔽发送者动作；不屏蔽消息内容，不改变屏蔽列表或资料页入口。 |
| 菜单隐藏统计 | `nagram.hideMenuStatistics` | 隐藏消息统计及投票统计入口；不更改频道管理员能力或投票数据。 |
| 菜单隐藏复制消息链接 | `nagram.hideMenuCopyLink` | 控制消息直达链接入口；普通正文链接的复制和打开仍按原行为处理。 |
| 菜单隐藏转发 | `nagram.hideMenuForward` | 覆盖单条及多选转发菜单，独立于消息旁快捷转发箭头的开关；不变更转发权限。 |
| 菜单隐藏翻译 | `nagram.hideMenuTranslate` | 覆盖全文、选中文字和投票内容的翻译菜单；不关闭已有聊天翻译状态或变更翻译服务。 |
| 菜单隐藏选择消息 | `nagram.hideMenuSelect` | 隐藏开始选择的菜单入口，保留取消现有选择，鼠标和键盘原生选择方式保持原样。 |
| 菜单隐藏表情包信息 | `nagram.hideMenuEmojiPacks` | 隐藏消息及相应反应、标签、投票选项中的表情包信息动作及其分隔线；不隐藏表情本身。 |

输入区适配同时覆盖普通聊天的旧输入实现和话题、计划消息等使用的 ComposeControls；菜单适配同时覆盖 HistoryInner 与 ListWidget。功能只控制本地展示和已有动作入口，沿用上游权限判断；不增加发送、举报或屏蔽请求。

验证记录：macOS arm64 Debug 构建通过。独立临时数据目录中使用合成会话，100% 缩放下 615 项检查、125% 和 200% 下各 618 项检查通过，合计 1851 项。覆盖 20 个键的默认值、独立设置实例、持久化往返、重复通知抑制与订阅生命周期；三语下 41 个开关和源码链接的页面构建、20 项搜索与实际点击；时间标记、签名和计数的像素对照；普通及计划消息输入区的显隐、宽度和计时器保留；9 类菜单动作的可见性、顺序恢复、多选退出和复制权限；两种文件夹布局的未读更新；表情互动与 Premium 特效请求门控、消息特效元数据和已读生命周期。111 条三语文案的键及占位符一致。125%/200% 保存了三语首屏截图，测试容器为透明设置页补齐标准窗口背景；已检查简繁文字、英文换行及开关显示。临时测试场景已恢复为空实现，交付 Debug 应用已重新构建。

验证边界：真实联网特效资源的解码和完整播放、播放中切换及下载完成竞态、HistoryInner 旧菜单与旧输入区的真实鼠标交互、深色主题、真实账号切换和 Windows/Linux 运行尚未端到端验收；相应接入点已完成代码检查。测试没有发送真实消息或触发菜单中的网络动作。其余 7 个预留键仍不显示为可用功能。

### 第五批：列表密度、贴纸尺寸与媒体发送确认

本批继续推进 P1-04/06/07，新增 5 个可用开关和 1 个数值选项。均为本机 D 作用域，支持三语搜索；不添加恢复默认入口，不修改登录数据目录。

| 配置键 | 功能与生效方式 |
| --- | --- |
| `nagram.chatListCompact` | 普通聊天行缩小行高、头像与文字间距，即时更新列表高度、搜索结果位置和鼠标命中；带标签、论坛、话题和窄侧栏保持原布局 |
| `nagram.stickerScale` | 50%–200%，每档 25%，默认 100%；使用现有 KV 存储，非法值记录错误并按 100% 读取，不覆盖原数据；消息与大图预览共用尺寸上限，表情、骰子及固定尺寸内容保持原大小 |
| `nagram.confirmStickers` | 普通聊天和话题中的贴纸选择、推荐及内联结果在发送前确认收件人；取消保留输入，确认回调仅消费一次 |
| `nagram.confirmGifs` | 收藏 GIF 与 GIF 搜索结果发送前确认；与贴纸开关独立，不影响普通文件和自定义表情 |
| `nagram.previewVoiceMessages` | 录音结束后进入上游试听、裁剪界面，由用户再次发送或丢弃 |
| `nagram.previewVideoMessages` | 圆视频录制结束后进入上游预览界面，由用户再次发送或丢弃 |

定时发送保留原有时间确认，业务欢迎语模板编辑不增加发送确认。确认后继续检查发送权限、慢速模式和付费要求；异步回调绑定发送界面生命周期，旧聊天切换后不继续发送。录制预览复用原有暂停、试听、裁剪和阅后即焚状态，不提前清除录音。

文件夹文字/图标模式及横竖布局复用“设置 → 聊天文件夹”中的上游能力，本批不注册同义配置。P1-04 的预览行数、归档与其他导航入口，P1-06 的转发评论顺序及问候贴纸，P1-07 的最近贴纸数量、完整媒体信息与下载动作仍需后续推进，不能将对应功能包整体标记完成。

验证记录：macOS arm64 Debug 构建通过。最终代码在 125% 和 200% 缩放下各通过 426 项离线检查，合计 852 项，覆盖全部 51 个布尔键的默认值、去重通知、持久化和实例隔离，7 档尺寸及非法值处理，三语的 48 个页面按钮（46 个开关、尺寸和源码入口）、5 个新增开关的搜索和实际点击、数值弹窗选择、普通与论坛混合行高和边界命中、新加入行的密度、窄侧栏、贴纸尺寸及表情尺寸隔离，以及发送确认的取消、显式确认、重复点击和发送界面生命周期。检查并保留了两档缩放的三语媒体页及列表截图；125% 测试发现并修正了内边距取整导致的 1 像素越界。125 条内置文案三语键及占位符一致。临时测试场景已恢复为空实现，交付 Debug 应用已重新构建。

验证边界：离线场景没有发送真实消息，也没有启动麦克风或摄像头。真实录制、裁剪及阅后即焚发送，联网付费/定时发送、旧聊天与话题的完整发送交互、深色主题和 Windows/Linux 尚未端到端验收；对应接入点完成代码检查。当前保留的 6 个布尔键为手机号备用键、发送前翻译、自动翻译、发送/编辑中西文间距和过滤总开关，均不显示为可用项。

每个选项接入前须确认：默认行为、切换生效、重启恢复、关闭后的行为还原、账号切换、继承/显式关闭、页面重开、搜索返回和无权限状态。UI 需覆盖多语言、深浅色和 125%/200% 缩放；文本变换需覆盖组合实体、代码、超长消息和撤销。

P2 的外部请求须测试取消、超时、凭据错误和不重复发送；正则须测试性能及不兼容表达式；结构化配置须测试坏数据、未知字段和幂等迁移。P3 需增加断线重连、跨账号、异步竞态、数据保留/清理、回退和外部能力不可用测试。

只有行为验收通过的项目才进入真实 UI。目录中的“纳入/合并”“条件纳入”和 P1/P2/P3 都是规划标识，不是已实现承诺；排除项只保留核对记录。

### 桌面共有功能行为对齐

- 头像圆角同步用于静态头像、视频帧、动态外圈和在线角标。默认保留论坛与频道私信形状；统一头像形状为独立选项，重启生效。原有 0 继承、10–100 百分比值保持兼容。
- 整条消息隐藏规则读取原文、相册说明、隐藏链接和按钮内容；显示语言切换不改变匹配依据。文字遮盖和替换继续只处理显示文字。已屏蔽来源覆盖直接作者、转发原作者和 inline bot，直接私聊历史保留。旧 `maskMessage` 在保存时规范为 `hide`，界面只有一种整条隐藏操作。
- 简化引用移除正文引用、回复、链接预览的底色及背景图案，动态转发也移除装饰；保留发送者配色。截图使用自己的样式实例，截图偏好不修改聊天样式。
- 资料 ID 提供关闭、Bot API 和原始格式；用户 ID 不变，Bot API 群 ID 为负值，频道／超级群 ID 使用对应的 Bot API 编码。
- `nagram.hideApplicationBadge` 独立控制应用角标，不依赖演示模式。演示模式接入现有窗口保护管理器，覆盖后来显示的窗口，并与原有受保护内容共同管理生命周期。系统捕获排除是请求，不保证所有捕获工具遵守。

验证记录：macOS arm64 Debug 构建通过；125% 和 200% 缩放下各通过 95 项离线检查，合计 190 项、0 失败，保存 20 张 PNG。覆盖新增偏好的默认值、持久化和配置交换，用户／群组／频道的两种 ID 格式，引用样式即时切换与截图样式隔离，静态／视频头像像素一致性、论坛形状及角标位置，实际翻译消息、隐藏 URL、按钮 URL、转发和 inline bot 来源过滤、解除屏蔽后的缓存刷新，原生窗口保护原因叠加及独立角标，三语设置页面和 ID 控件，以及旧规则迁移后连续保存。482 条 Nagram 文案的三语键、重复键与占位符一致。证据位于 `out/nagram-align125-final-evidence`、`out/nagram-align200-final-evidence`；临时测试场景恢复为空实现后重新构建交付应用。

验证边界：窗口捕获保护已验证管理器状态与多原因生命周期，未对实际录屏工具做端到端排除验收。Windows/Linux、真实视频解码播放和动态转发界面的完整交互未运行；相应接入点完成代码检查。测试未连接真实账号或发送消息。

交付构建日志为 `out/nagram-align-production-build.log`；应用签名校验及隔离目录启动检查通过，运行日志确认测试场景为空。原有登录目录未改动，测试账号目录已清理。
